{
  "name": "smallorange-s3-proxy",
  "version": "1.0.5",
  "description": "Small proxy to get stuffs and copy to s3 and retrieve for there",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test ./node_modules/.bin/_mocha *.spec.js --recursive --watch --timeout 5000",
    "coverage": "NODE_ENV=test ./node_modules/.bin/istanbul cover -x *.spec.js ./node_modules/.bin/_mocha -- *.spec.js --recursive --timeout 5000",
    "deploy": "npm version patch -m 'Bump %s' && git push origin master && npm publish"
  },
  "author": "Felipe Rohde",
  "license": "ISC",
  "dependencies": {
    "got": "^8.3.0",
    "istanbul": "^0.4.5",
    "lodash": "^4.17.4",
    "md5": "^2.2.1",
    "mime": "^2.2.0",
    "sinon": "^4.4.6"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.0.4",
    "sinon-chai": "^3.0.0"
  },
  "repository": "https://github.com/feliperohdee/smallorange-s3-proxy.git"
}
